#!/usr/bin/env ruby

require 'spacefind'

if ARGV.length < 1
  puts "Usage: spacefind <phpfile(s)>"
  exit(-1)
end

ARGV.each do |f|
  if Spacefind.trailing_whitespace?(f)
    puts "May contain whitespace: #{f}"
  end
end
